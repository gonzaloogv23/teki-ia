rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Permite lectura a todos
      allow read: if true;

      // Permite escritura a todos
      allow write: if true;
    }
  }
}
