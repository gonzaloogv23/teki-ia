{
  "name": "teki-ia",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node api/proxy.js",
    "install": "cd frontend && npm install && cd .. && cd api && npm install",
    "build": "cd frontend && npm run build || (cd frontend && npx react-scripts build)"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.21.1",
    "http-proxy-middleware": "^2.0.4",
    "multer": "^1.4.5-lts.1",
    "pdf-parse": "^1.1.1"
  },
  "devDependencies": {
    "@babel/plugin-proposal-private-property-in-object": "^7.18.6"
  },
  "engines": {
    "node": ">=14.x"
  }
}
